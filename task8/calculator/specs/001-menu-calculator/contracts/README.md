# Contracts for Menu-Driven Basic Calculator

This is a Command Line Interface (CLI) application and does not expose any external APIs or formal contracts. All interactions are handled directly via standard input/output.

Therefore, this directory is created for consistency but contains no specific contract definitions.